import React from 'react';

const MinimalApp: React.FC = () => {
  return (
    <div className="flex h-screen w-screen items-center justify-center bg-blue-500 text-white font-bold text-2xl">
      Debug Mode: Minimal UI Rendered Successfully
    </div>
  );
};

export default MinimalApp;
